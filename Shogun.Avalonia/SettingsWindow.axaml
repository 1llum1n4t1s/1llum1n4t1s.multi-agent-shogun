<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Shogun.Avalonia.ViewModels"
        xmlns:util="clr-namespace:Shogun.Avalonia.Util"
        mc:Ignorable="d" d:DesignWidth="520" d:DesignHeight="450"
        x:Class="Shogun.Avalonia.SettingsWindow"
        x:DataType="vm:SettingsViewModel"
    Title="設定 - Shogun.Avalonia"
    Width="550" Height="700"
    WindowStartupLocation="CenterScreen"
    CanResize="True">
    <Window.Resources>
        <util:BoolToColorConverter x:Key="LoginColorConverter" TrueColor="Green" FalseColor="Red"/>
    </Window.Resources>
    <Grid RowDefinitions="Auto,*" Margin="16">
        <TextBlock Grid.Row="0" Text="設定" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="12" Margin="0,0,4,0">
                <!-- config/settings.yaml: skill.local_path -->
                <StackPanel Spacing="4">
                    <TextBlock Text="ローカルスキル保存先 (skill.local_path)"/>
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBox Grid.Column="0" Text="{Binding SkillLocalPath}" IsReadOnly="True" Watermark="参照でフォルダを選択"/>
                        <Button Grid.Column="1" Content="参照" Margin="8,0,0,0" Click="OnBrowseSkillLocalPath"/>
                    </Grid>
                </StackPanel>

                <!-- config/settings.yaml: screenshot.path -->
                <StackPanel Spacing="4">
                    <TextBlock Text="スクリーンショット保存先 (screenshot.path)"/>
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBox Grid.Column="0" Text="{Binding ScreenshotPath}" IsReadOnly="True" Watermark="参照でフォルダを選択"/>
                        <Button Grid.Column="1" Content="参照" Margin="8,0,0,0" Click="OnBrowseScreenshotPath"/>
                    </Grid>
                </StackPanel>

                <!-- config/settings.yaml: logging.level。本アプリの Logger は未使用。upstream の config 用。 -->
                <StackPanel Spacing="4">
                    <TextBlock Text="ログレベル (logging.level)"/>
                    <ComboBox ItemsSource="{Binding LogLevelOptions}" SelectedItem="{Binding LogLevel}" MinWidth="140" HorizontalAlignment="Left"/>
                    <TextBlock Text="※ config/settings.yaml に保存。upstream の CLI・スクリプトで参照。本アプリのログ出力レベルはビルド時に固定（Debug/Release で Info）。" FontSize="11" Foreground="Gray" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- config/settings.yaml: logging.path -->
                <StackPanel Spacing="4">
                    <TextBlock Text="ログ保存先 (logging.path)"/>
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBox Grid.Column="0" Text="{Binding LogPath}" IsReadOnly="True" Watermark="参照でフォルダを選択"/>
                        <Button Grid.Column="1" Content="参照" Margin="8,0,0,0" Click="OnBrowseLogPath"/>
                    </Grid>
                </StackPanel>

                <!-- 足軽人数（アプリ固有） -->
                <TextBlock Text="エージェント（アプリ固有）" FontWeight="SemiBold" Margin="0,8,0,0"/>
                
                <!-- 家老の権限設定 -->
                <StackPanel Spacing="4">
                    <TextBlock Text="家老のコード改修 権限モード" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding KaroPermissionModeOptions}" SelectedItem="{Binding KaroExecutionPermissionMode}" MinWidth="200" HorizontalAlignment="Left">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <TextBlock Text="• AlwaysAllow: 家老がコード改修を自動実行
• AlwaysReject: 家老は分析のみ実行
• PromptUser: 各改修時に許可/却下を確認（推奨）" FontSize="11" Foreground="Gray" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Spacing="4">
                    <TextBlock Text="足軽の人数（1～20）"/>
                    <ComboBox ItemsSource="{Binding AshigaruCountOptions}" SelectedItem="{Binding AshigaruCount}" MinWidth="80" HorizontalAlignment="Left"/>
                    <TextBlock Text="※ 変更後はアプリ再起動を推奨" FontSize="11" Foreground="Gray"/>
                </StackPanel>

                <!-- 各エージェント用モデル（必須・初期は haiku）。ドロップダウンは MODEL 表示、選択のみ。 -->
                <TextBlock Text="エージェント用モデル（必須）" FontWeight="SemiBold" Margin="0,8,0,0"/>
                <StackPanel Spacing="4">
                    <TextBlock Text="将軍用モデル（殿の入力→家老への指示文生成）"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <ComboBox ItemsSource="{Binding AllModelOptions}" SelectedItem="{Binding SelectedModelShogun}" IsEditable="False" MinWidth="260" DisplayMemberBinding="{Binding DisplayName}"/>
                        <CheckBox Content="(Thinking)" IsChecked="{Binding ThinkingShogun}" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Spacing="4">
                    <TextBlock Text="家老用モデル"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <ComboBox ItemsSource="{Binding AllModelOptions}" SelectedItem="{Binding SelectedModelKaro}" IsEditable="False" MinWidth="260" DisplayMemberBinding="{Binding DisplayName}"/>
                        <CheckBox Content="(Thinking)" IsChecked="{Binding ThinkingKaro}" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Spacing="4">
                    <TextBlock Text="足軽用モデル"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <ComboBox ItemsSource="{Binding AllModelOptions}" SelectedItem="{Binding SelectedModelAshigaru}" IsEditable="False" MinWidth="260" DisplayMemberBinding="{Binding DisplayName}"/>
                        <CheckBox Content="(Thinking)" IsChecked="{Binding ThinkingAshigaru}" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <!-- ドキュメントルート設定 -->
                <TextBlock Text="ドキュメント出力設定" FontWeight="SemiBold" Margin="0,8,0,0"/>
                <StackPanel Spacing="4">
                    <TextBlock Text="ドキュメントルート (DocumentRoot)"/>
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBox Grid.Column="0" Text="{Binding DocumentRoot}" IsReadOnly="True" Watermark="参照でフォルダを選択"/>
                        <Button Grid.Column="1" Content="参照" Margin="8,0,0,0" Click="OnBrowseDocumentRoot"/>
                    </Grid>
                    <TextBlock Text="※ 報告書等の出力先。プロジェクト選択時は「ドキュメントルート\プロジェクト名」に出力されます。" FontSize="11" Foreground="Gray" TextWrapping="Wrap"/>
                </StackPanel>

                <Border Height="24"/>
                <!-- ボタン -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                    <Button Content="キャンセル" Command="{Binding CancelCommand}"/>
                    <Button Content="保存して閉じる" Command="{Binding SaveAndCloseCommand}"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
